# Orchestrator backend requirements.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.6.0

# Vertex AI GenAI SDK (Gemini models).
google-genai>=1.0.0

# MCP client (Streamable HTTP transport to the MCP servers).
mcp>=1.2.0

# Minting Cloud Run identity tokens to call --no-allow-unauthenticated services.
google-auth>=2.30.0
