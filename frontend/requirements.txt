# Frontend (chat UI) requirements.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
requests
# Proxying to the orchestrator.
httpx>=0.27.0

# Minting Cloud Run identity tokens to call the private orchestrator.
google-auth>=2.30.0
